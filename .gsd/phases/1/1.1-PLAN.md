---
phase: 1
plan: 1
wave: 1
---

# Plan 1.1: Project Setup & Fluid UI Foundation

## Objective
Initialize the Next.js project with TypeScript and Tailwind CSS, and implement the core "Future Fashion" fluid glassmorphic UI foundation as the base for all subsequent features.

## Context
- .gsd/SPEC.md
- .gsd/ROADMAP.md
- UI Research (Framer Motion + Glassmorphism)

## Tasks

<task type="auto">
  <name>Initialize Next.js App & Dependencies</name>
  <files>
    - package.json
    - tailwind.config.ts
    - next.config.ts
  </files>
  <action>
    - Run `npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*"`
    - Install additional dependencies: `framer-motion`, `lucide-react`, `@supabase/supabase-js`, `@supabase/ssr`, `clsx`, `tailwind-merge`.
    - Update `tailwind.config.ts` to include backdrop-blur and custom animation utilities if needed.
  </action>
  <verify>npm run build</verify>
  <done>Next.js app is initialized and builds successfully with all dependencies installed.</done>
</task>

<task type="auto">
  <name>Implement Fluid Background & Base Components</name>
  <files>
    - src/styles/globals.css
    - src/components/ui/FluidBackground.tsx
    - src/components/ui/GlassCard.tsx
    - src/app/layout.tsx
  </files>
  <action>
    - Add Glassmorphism utility classes to `globals.css` (frosted glass, gradients).
    - Create `FluidBackground.tsx` using Framer Motion (animated blobs).
    - Create `GlassCard.tsx` as a reusable container.
    - Update `layout.tsx` to include the `FluidBackground` and set up the font (Inter/Outfit).
  </action>
  <verify>Manual visual check (via browser tool in subsequent execution phase)</verify>
  <done>The application has a premium, flowy, glassmorphic background visible on all pages.</done>
</task>

## Success Criteria
- [ ] Next.js + Tailwind project structure established.
- [ ] Dependencies installed.
- [ ] Global UI foundation (Fluid Bg + Glassmorphism) implemented.
