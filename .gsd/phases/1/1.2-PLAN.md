---
phase: 1
plan: 1
wave: 2
---

# Plan 1.2: Supabase Schema & Auth Configuration

## Objective
Configure Supabase and implement the core database schema with Row Level Security (RLS) to support multi-role authentication (Admin/Employee).

## Context
- .gsd/SPEC.md
- .gsd/REQUIREMENTS.md
- Supabase SQL Schema (init_schema.sql)

## Tasks

<task type="auto">
  <name>Initialize Supabase DB Schema & RLS</name>
  <files>
    - supabase/migrations/20240405000000_init_schema.sql
  </files>
  <action>
    - Define tables: `profiles`, `tasks`, `logs`, `notifications`.
    - Setup RLS policies for each table (Admin/Employee rules).
    - Setup Trigger to auto-create `profiles` on Supabase Auth Signup.
  </action>
  <verify>Manual SQL query check (via Supabase MCP or CLI if available)</verify>
  <done>Database schema is initialized with proper RLS policies.</done>
</task>

<task type="auto">
  <name>Configure Supabase Client & Auth Helpers</name>
  <files>
    - src/utils/supabase/client.ts
    - src/utils/supabase/server.ts
    - src/utils/supabase/middleware.ts
  </files>
  <action>
    - Create Supabase client instances for Client Components, Server Components, and Middleware.
    - Setup type-safe database client using `@supabase/supabase-js`.
  </action>
  <verify>npm run build</verify>
  <done>Supabase client is configured and integrated into the project.</done>
</task>

## Success Criteria
- [ ] Database schema is active on Supabase.
- [ ] RLS policies are in place.
- [ ] Supabase Auth helpers are ready for use.
