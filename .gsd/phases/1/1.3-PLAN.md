---
phase: 1
plan: 1
wave: 3
---

# Plan 1.3: RBAC Middleware & Login Flow

## Objective
Implement a secure, role-aware routing layer using Next.js Middleware and build the primary authentication entry point for the application.

## Context
- src/utils/supabase/middleware.ts
- src/app/login/page.tsx
- .gsd/SPEC.md

## Tasks

<task type="auto">
  <name>Implement Next.js Middleware for RBAC</name>
  <files>
    - src/middleware.ts
  </files>
  <action>
    - Authenticate user on every request.
    - Fetch user role from `profiles` table.
    - Redirect `/admin/*` to `/login` if not admin.
    - Redirect `/employee/*` to `/login` if not employee.
    - Redirect authenticated users from `/login` to their respective dashboards.
  </action>
  <verify>npm run build</verify>
  <done>Middleware correctly handles authentication and role-based redirects.</done>
</task>

<task type="auto">
  <name>Build Login Page with Fluid UI</name>
  <files>
    - src/app/login/page.tsx
  </files>
  <action>
    - Create a glassmorphic login form (email/password).
    - Implement Login logic using Supabase.
    - Style with Tailwind CSS + Framer Motion (animated entry/exit).
    - Focus on accessibility and user experience.
  </action>
  <verify>Manual visual check (via browser tool in execution phase)</verify>
  <done>Login page is fully functional and visually consistent with the theme.</done>
</task>

## Success Criteria
- [ ] Users can log in with email/password.
- [ ] Admins are redirected to `/admin/dashboard`.
- [ ] Employees are redirected to `/employee/dashboard`.
- [ ] Unauthorized access to dashboards is blocked.
