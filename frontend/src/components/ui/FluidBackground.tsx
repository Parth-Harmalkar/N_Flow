"use client";

// FluidBackground is now a no-op — the dark theme is handled entirely
// via globals.css body background. This component is kept for import
// compatibility but renders nothing.
export default function FluidBackground() {
  return null;
}